import { TokenExpiredError } from "jsonwebtoken"

const refreshToken = () => {
    //await  call decision to create a new access Token
    // return getTEAccessToken()
}

export default refreshToken
